version: v1
breaking:
  use:
    - WIRE_JSON
  ignore:
    # Do not enforce breaking change detection for the experimental package
    - khulnasoft/experimental
    # TODO enable again after landing style changes
    - khulnasoft/v1
lint:
  use:
    - DEFAULT
  ignore:
    - google
